from pynecone import Cmd


class {{ class_name }}(Cmd):

        def __init__(self):
            super().__init__({{ name }})

        def add_arguments(self, parser):
            parser.add_argument('op', choices=['one', 'two'],
                                help="one or two", default='two', const='two', nargs='?')

        def run(self, args):
            pass

        def get_help(self):
            return 'help'
